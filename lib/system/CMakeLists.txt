if (EXISTS "${CMAKE_CURRENT_SOURCE_DIR}/${PROJECT_SYSTEM}/CMakeLists.txt"
    AND EXISTS "${CMAKE_CURRENT_SOURCE_DIR}/${PROJECT_SYSTEM}/machine/${PROJECT_MACHINE}/CMakeLists.txt")
  add_subdirectory (${PROJECT_SYSTEM})
endif ()
